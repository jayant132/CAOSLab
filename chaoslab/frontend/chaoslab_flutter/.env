# Default backend URL for local development. Edit if your backend runs
# elsewhere (see .env.example for platform-specific notes: emulator vs
# physical device vs desktop/web).
API_BASE_URL=http://127.0.0.1:8000
